[project]
name = "cargopy"
version = "0.1.0"
description = "Rust Cargo-like project manager in Python"
readme = "README.md"
requires-python = ">= 3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
